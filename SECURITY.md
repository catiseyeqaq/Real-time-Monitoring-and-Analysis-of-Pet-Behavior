# 安全说明

## 当前 Demo 的安全边界

- 默认只监听 `127.0.0.1`，不会自动开启公网映射。
- API Key、账号信息、日志和临时音频保存在本机运行目录，仅适合个人本机 Demo。
- 不要把 `.gradio/`、图片、音频、模型权重、数据集或日志提交到公共仓库。
- 不要在 issue、截图或日志中公开真实 API Key、家庭地址、宠物主人信息或串口硬件标识。

## 商用部署前

请替换为密码哈希、密钥管理服务、最小权限访问、HTTPS、审计日志、数据保留策略和异常告警；对自动投喂、舵机和报警增加硬件限位、急停、断连保护和人工接管。

公网演示必须显式启用 FRP，并配置独立令牌、访问控制和服务端防火墙。不要把 FRP 令牌写入代码、配置文件或提交历史。

发现安全问题时，请不要公开发布凭据或可利用细节，先通过仓库维护者可用的私密渠道联系，并提供复现步骤、影响范围和修复建议。
