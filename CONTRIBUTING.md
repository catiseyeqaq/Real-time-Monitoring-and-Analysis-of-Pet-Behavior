# 贡献指南

感谢你为智能宠物行为识别与环境联动系统贡献代码、文档或硬件适配。

## 提交前检查

- 运行 `python -m py_compile pet_behavior_gradio_app.py start_app.py setup_environment.py`。
- 如果修改了串口协议，请同步更新 `HARDWARE_SERIAL_PROTOCOL.md`。
- 如果修改了页面功能，请同步更新 `README.md` 和首次使用说明。
- 不要提交 API Key、账号密码、`.gradio/` 数据库、个人图片/音频、训练数据、模型权重和运行产物。
- 不要把真实宠物健康信息、家庭地址或串口设备唯一标识写入 issue 和日志样例。

## 提交问题

请尽量包含操作系统、Python/依赖版本、启动命令、是否连接 CH340、可复现步骤、完整错误信息和脱敏后的日志。模型结果问题还应说明输入类型、类别配置和模型来源，不要直接上传受限数据。

## 代码约定

- 保持函数职责清晰，优先复用现有日志、会话状态和串口 JSON 解析逻辑。
- 用户界面文案应围绕宠物行为分析、照护建议和硬件联动，不要把底层模型品牌当作项目功能宣传。
- 涉及自动投喂、舵机和报警的改动必须考虑超限、断连、空值和异常返回。
- 外部 API Key 只能从参数、环境变量或本地配置读取，禁止硬编码。

## 许可证

本项目按 [AGPL-3.0](LICENSE) 发布。提交代码即表示你有权按该许可证贡献相应内容，并理解网络服务、衍生作品和第三方依赖可能产生的合规义务。商用部署请先阅读 [COMMERCIAL_USE.md](COMMERCIAL_USE.md)。

