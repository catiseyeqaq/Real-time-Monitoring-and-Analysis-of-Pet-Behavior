# CH340 串口硬件对接说明

本文档给下位机硬件工程师使用，用于把开发板通过 CH340 / CH341 USB 串口模块接入当前本地宠物行为识别系统。

## 1. 当前系统连接方式

上位机运行 Gradio 本地网页：

```text
http://127.0.0.1:7861/
```

网页中的硬件入口：

- `CH340 硬件收发测试`
- `本地联动控制`

上位机通过 Python `pyserial` 打开 Windows COM 口，与开发板进行串口文本通信。

## 2. 串口参数

默认参数如下：

| 参数 | 默认值 | 说明 |
|---|---:|---|
| 串口芯片 | CH340 / CH341 | Windows 设备管理器通常显示为 `USB-SERIAL CH340 (COMx)` |
| 端口 | 页面选择 | 例如 `COM3` |
| 波特率 | `115200` | 页面可修改 |
| 数据位 | `8` | pyserial 默认 |
| 校验位 | `None` | pyserial 默认 |
| 停止位 | `1` | pyserial 默认 |
| 流控 | 无 | 不使用 RTS/CTS |
| 编码 | UTF-8 文本 | JSON 字符串 |
| 帧结束 | `\n` 换行 | 页面默认追加换行 |
| 读取等待 | `0.5s` | 页面可在 `0.1s - 3.0s` 间调整 |

开发板建议按“收到一行 JSON 后解析并返回一行 JSON”的方式实现。

## 3. 上位机发送流程

每次发送时，上位机会：

1. 打开指定 COM 口。
2. 清空输入缓冲区。
3. 发送 UTF-8 编码 JSON 文本。
4. 默认在末尾追加 `\n`。
5. 等待页面设置的读取时间，默认 `0.5s`。
6. 读取串口返回内容。
7. 关闭串口。

因此下位机需要在收到命令后尽快返回，建议响应时间控制在 `500ms` 内。

## 4. 基础测试命令

页面原始发包默认测试：

```json
{"cmd":"ping"}
```

建议下位机返回：

```json
{"ok":true,"cmd":"ping","message":"pong"}
```

如果页面能看到 `pong`，说明串口链路、波特率和换行解析正常。

## 5. 温度读取协议

上位机点击 `读取温度` 或执行本地联动时，会发送：

```json
{"cmd":"temperature","action":"read"}
```

下位机推荐返回：

```json
{"ok":true,"cmd":"temperature","temperature_c":28.5}
```

上位机可解析以下任意字段作为温度值：

- `temperature`
- `temperature_c`
- `temp`
- `value`

推荐使用 `temperature_c`，单位为摄氏度。

## 6. 重量读取协议

执行 `本地联动控制` 且页面“当前食物重量 kg”留空时，上位机会发送：

```json
{"cmd":"weight","action":"read"}
```

下位机推荐返回：

```json
{"ok":true,"cmd":"weight","weight_kg":4.2}
```

上位机可解析以下任意字段作为重量值：

- `weight`
- `weight_kg`
- `kg`
- `value`

推荐使用 `weight_kg`，单位为 kg。

## 7. 舵机控制协议

手动舵机测试会发送：

```json
{"cmd":"servo","id":1,"angle":90}
```

字段说明：

| 字段 | 类型 | 说明 |
|---|---|---|
| `cmd` | string | 固定为 `servo` |
| `id` | int | 舵机编号，默认 `1` |
| `angle` | int | 舵机角度，页面范围 `0 - 180` |

下位机推荐返回：

```json
{"ok":true,"cmd":"servo","id":1,"angle":90}
```

## 8. 自动投放控制码

`本地联动控制` 中默认的自动投放舵机控制码为：

```json
{"cmd":"servo","id":1,"angle":90}
```

该控制码可以在页面中修改。如果硬件工程师需要不同协议，例如打开投喂仓 `800ms`，也可以约定为：

```json
{"cmd":"feed","servo_id":1,"open_ms":800}
```

但如果修改为自定义协议，下位机必须按页面中填写的 JSON 解析。

## 9. 温度报警协议

本地联动中默认温度报警阈值为：

```text
35.0 °C
```

当温度值大于或等于该阈值时，上位机会触发报警记录，并发送页面中的报警控制码。默认报警控制码为：

```json
{"cmd":"alarm","level":"high"}
```

下位机推荐返回：

```json
{"ok":true,"cmd":"alarm","level":"high"}
```

硬件侧可把该命令映射到蜂鸣器、LED、屏幕提示或继电器。

## 10. 本地联动逻辑

页面 `本地联动控制` 的默认参数：

| 参数 | 默认值 | 说明 |
|---|---:|---|
| 单轮投放上限 | `5.0 kg` | 超过后阻止舵机，防止猫吃太多 |
| 吃食触发重量下降 | `0.02 kg` | 当前重量比上次重量下降超过该值，认为猫吃了食物 |
| 温度报警阈值 | `35.0 °C` | 大于或等于该值触发报警 |
| 自动投放控制码 | `{"cmd":"servo","id":1,"angle":90}` | 触发投放时发送 |
| 报警控制码 | `{"cmd":"alarm","level":"high"}` | 温度过高时发送 |

联动顺序：

1. 读取重量。
2. 读取温度。
3. 如果温度过高，先报警，不投放。
4. 第一次运行只记录初始重量，不投放。
5. 下一次运行时，如果重量下降超过阈值，认为猫吃了食物。
6. 如果累计投放估算没有超过 `5kg`，发送舵机投放控制码。
7. 如果累计投放估算超过 `5kg`，阻止舵机并记录报警。

## 11. 推荐下位机返回格式

所有返回都建议使用单行 JSON，末尾带 `\n`：

成功：

```json
{"ok":true,"cmd":"temperature","temperature_c":28.5}
```

失败：

```json
{"ok":false,"cmd":"temperature","error":"sensor_timeout"}
```

上位机当前重点解析数值字段，不强制要求 `ok`，但保留 `ok/cmd/error` 有利于调试。

## 12. Arduino / ESP32 伪代码

```cpp
void loop() {
  if (!Serial.available()) {
    return;
  }

  String line = Serial.readStringUntil('\n');
  line.trim();

  if (line.indexOf("\"cmd\":\"ping\"") >= 0) {
    Serial.println("{\"ok\":true,\"cmd\":\"ping\",\"message\":\"pong\"}");
  } else if (line.indexOf("\"cmd\":\"temperature\"") >= 0) {
    float t = readTemperatureC();
    Serial.print("{\"ok\":true,\"cmd\":\"temperature\",\"temperature_c\":");
    Serial.print(t, 2);
    Serial.println("}");
  } else if (line.indexOf("\"cmd\":\"weight\"") >= 0) {
    float w = readWeightKg();
    Serial.print("{\"ok\":true,\"cmd\":\"weight\",\"weight_kg\":");
    Serial.print(w, 3);
    Serial.println("}");
  } else if (line.indexOf("\"cmd\":\"servo\"") >= 0) {
    int angle = parseAngle(line);
    servo.write(angle);
    Serial.print("{\"ok\":true,\"cmd\":\"servo\",\"id\":1,\"angle\":");
    Serial.print(angle);
    Serial.println("}");
  } else if (line.indexOf("\"cmd\":\"alarm\"") >= 0) {
    setAlarm(true);
    Serial.println("{\"ok\":true,\"cmd\":\"alarm\",\"level\":\"high\"}");
  } else {
    Serial.println("{\"ok\":false,\"error\":\"unknown_cmd\"}");
  }
}
```

实际工程中建议使用 ArduinoJson 或类似 JSON 库解析，不建议长期使用字符串匹配。

## 13. 上位机调试步骤

1. 插入 CH340。
2. Windows 设备管理器确认端口，例如 `COM3`。
3. 打开本地网页 `http://127.0.0.1:7861/`。
4. 进入 `CH340 硬件收发测试`。
5. 点击 `刷新串口`，选择 `COM3`。
6. 原始发包输入 `{"cmd":"ping"}`，点击发送。
7. 确认返回 `pong`。
8. 点击 `读取温度`，确认返回温度 JSON。
9. 进入 `本地联动控制`，先手动填重量和温度测试逻辑。
10. 再清空手动重量/温度，让系统从开发板读取真实传感器数据。

## 14. 常见问题

### 页面看不到 COM 口

确认 Python 环境安装了 `pyserial`：

```powershell
python -m pip install pyserial
python -c "import serial.tools.list_ports as p; [print(x.device, x.description) for x in p.comports()]"
```

### 能看到 COM 口但没有返回

检查：

- 波特率是否为 `115200`
- 下位机是否按 `\n` 分帧
- 下位机是否返回 UTF-8 文本
- 开发板串口是否与 CH340 的 TX/RX 交叉连接
- GND 是否共地

### 重量或温度无法解析

请返回推荐字段：

```json
{"weight_kg":4.2}
{"temperature_c":28.5}
```

不要只返回带中文单位的字符串，例如 `当前重量4.2公斤`，虽然上位机会尝试提取数字，但不利于稳定对接。
