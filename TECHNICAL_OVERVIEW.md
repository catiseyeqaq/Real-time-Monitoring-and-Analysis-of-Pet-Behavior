# 技术方案说明

## 1. 系统定位

本项目是一个本地运行的宠物行为分析与环境联动系统，用于家庭宠物照护场景的功能验证、算法联调和硬件原型开发。系统将视觉、音频、传感器、规则引擎和控制输出组合成一条可追踪的本地流程。

## 2. 功能模块

### 2.1 图像行为分析

输入可以来自本机摄像头、USB 摄像头或图片文件。系统先使用本地行为模型生成类别、置信度和边界框，再将结构化结果与图像一起交给可选的多模态接口，输出行为判断、潜在风险和照护建议。类别集合由模型和训练数据决定，应用不会假设所有模型都支持相同类别。

### 2.2 猫叫音频理解

系统支持上传或录制音频，读取文件元信息后直接进行声音行为理解。输出包括 hungry、attention、fear、pain、warning、fighting、mating、relaxed 和 unknown 等标签、置信度、紧急程度和建议。该流程不把猫叫强制转换成人类语言文本。

### 2.3 环境传感器与执行器

上位机通过 CH340/CH341 串口与下位机交换单行 JSON：温度与湿度用于环境展示和报警判断，重量用于估算食物变化，舵机用于投喂动作，报警命令用于驱动蜂鸣器、指示灯或其他输出。

### 2.4 本地联动规则

默认联动顺序为：读取重量、温度和湿度；温度达到阈值时优先报警并阻止投放；首次运行保存重量基线；后续重量下降超过阈值时判断可能发生进食；未超过本轮投放上限时才发送投喂控制码。

所有阈值都应根据宠物、粮食和硬件重新标定，不能直接作为产品安全参数。

## 3. 数据与隐私

原始图片、音频、日志、账号信息和 API Key 默认保存在本机运行目录。应用默认监听 `127.0.0.1`，不主动开启公网访问。正式部署时应增加密码哈希、密钥管理、HTTPS、访问控制、审计、数据删除和备份策略。

## 4. 可替换边界

视觉模型、音频分析接口、串口协议和规则阈值均通过应用层封装，可以分别替换。替换模型时需要保持类别名称和输出结构一致；替换云端接口时需要重新检查数据出境、费用、限流和隐私条款。

## 5. 非目标与风险

本项目不提供兽医诊断，不保证行为识别准确率，也不保证硬件在任意异常情况下安全动作。涉及持续呕吐、疼痛、呼吸异常、中毒、外伤或其他紧急情况时，应立即联系专业兽医。自动投喂必须配置限位、急停、断连保护和人工接管机制。
