# =============================================================
# 关于本文件（请先阅读）
#
# 本仓库为个人作品集与能力展示用途，保留所有权利，未授予任何开源许可。
# 本依赖清单仅用于说明项目的技术栈与运行环境组成。
# 本仓库不提供模型权重、训练/评测数据、私有配置与部署凭据，
# 因此即便安装下列依赖，也无法直接运行或复现本项目。
#
# 如需可运行版本或商业授权，请联系仓库作者。
# =============================================================

# Core ML & Vision
ultralytics>=8.4.0
torch>=2.0.0
torchvision>=0.15.0
opencv-python>=4.8.0
numpy>=1.24.0
Pillow>=10.0.0

# Gradio Web UI
# Gradio 6.0 移除了 gr.update 等 API，本项目按 Gradio 4/5 接口编写，故锁定版本范围
gradio>=4.44.0,<6.0

# Serial communication (optional, for ESP32 hardware)
pyserial>=3.5
