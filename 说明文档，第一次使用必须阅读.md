# 智能宠物行为识别系统：首次使用说明

本文档用于本地 Demo 的安装、启动、账号配置和硬件联调。

## 1. 环境要求

- Windows 10/11
- Python 3.10–3.12，建议 Python 3.12
- Chrome 或 Edge
- 本机摄像头或 USB 摄像头（可选）
- CH340/CH341 USB 串口模块及传感器/舵机（可选）
- DashScope API Key（可选，仅云端语义分析需要）
- 与项目行为类别匹配的本地视觉模型权重（图片行为分析需要）

## 2. 安装依赖

在项目根目录执行：

```powershell
python -m pip install --upgrade pip
python -m pip install -r requirements.txt
```

也可以执行：

```powershell
python setup_environment.py
```

该脚本会创建或复用现有的 `yolocat` Conda 环境。环境名称是历史兼容命名，不代表仓库首页展示内容。

## 3. 准备本地模型

大体积模型权重和训练产物默认不提交到 Git。启动时显式传入自己的权重：

```powershell
python .\pet_behavior_gradio_app.py --weight .\path\to\your\best.pt
```

权重类别名称应与训练数据一致。模型输出仅用于辅助分析，不构成准确率或医疗结论保证。

## 4. 启动 Demo

```powershell
python .\start_app.py
```

或直接执行：

```powershell
python .\pet_behavior_gradio_app.py
```

默认访问 `http://127.0.0.1:7861/`。应用默认只监听本机，不启用公网映射；端口被占用时会自动切换。

## 5. 本地账号与 API Key

首次进入页面填写本地账号和密码即可创建用户。DashScope API Key 为可选配置：

- 配置后：启用图片语义分析和猫叫情绪分析。
- 不配置：仍可使用本地行为检测、音频元信息、串口测试和本地联动规则。

当前 Demo 会将本地账号信息和 API Key 保存到 `.gradio/pet_behavior_users.sqlite3`，仅适合个人本机验证。不要将该数据库提交到 Git，也不要直接把当前登录系统暴露到公网或用于多租户生产环境。

## 6. 功能入口

- `图片行为分析`：上传图片或使用摄像头，查看行为标注、检测详情、环境状态和照护建议。
- `音频叫声分析`：上传或录制猫叫，查看音频元信息和情绪/紧急程度分析。
- `CH340 硬件收发测试`：刷新串口、发送 JSON、读取温度/湿度和发送舵机指令。
- `本地联动控制`：按重量变化判断进食事件，按投放上限和温度阈值发送投喂或报警控制码。

## 7. 常见问题

### API 分析失败

确认 API Key、接口地址和模型名称正确，并在页面日志/终端日志中查看错误。接口调用会产生云服务费用并受服务商条款约束。

### 串口不可用

确认设备管理器出现 `USB-SERIAL CH340 (COMx)`，然后安装：

```powershell
python -m pip install pyserial
python -c "import serial.tools.list_ports as p; print([(x.device, x.description) for x in p.comports()])"
```

完整协议见 [HARDWARE_SERIAL_PROTOCOL.md](HARDWARE_SERIAL_PROTOCOL.md)。

### 识别结果不准确

检查模型类别、训练数据、输入图像和权重版本。行为识别只能作为辅助信息；涉及持续呕吐、疼痛、呼吸异常等情况，应由兽医确认。
